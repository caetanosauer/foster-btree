#include <slot_array.h>

int main(int argc, char ** argv)
{
    (void) argc;
    (void) argv;

    /* TODO
     * No main function defined yet. Also no other cpp files to compile,
     * as project is so far header-only.
     */

    return 0;
}
